<<<<<<< HEAD
version: "3.9"

services:
  competitor-intel:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: competitor-intelligence
    ports:
      - "8000:8000"   # FastAPI backend
      - "8501:8501"   # Streamlit frontend
    environment:
      - GROQ_API_KEY=${GROQ_API_KEY:-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}
    volumes:
      - ./data:/app/data         # Persist database
      - ./reports:/app/reports   # Persist PDF reports
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8000/health"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 30s
=======
version: "3.9"

services:
  competitor-intel:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: competitor-intelligence
    ports:
      - "8000:8000"   # FastAPI backend
      - "8501:8501"   # Streamlit frontend
    environment:
      - GROQ_API_KEY=${GROQ_API_KEY:}
    volumes:
      - ./data:/app/data         # Persist database
      - ./reports:/app/reports   # Persist PDF reports
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8000/health"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 30s
>>>>>>> c8b6483 (updated the report and fixed bugs)
